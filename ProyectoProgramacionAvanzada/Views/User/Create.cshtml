@model ProyectoProgramacionAvanzada.Models.User

@{
    ViewBag.Title = "Crear Usuario";
}

<h2>@ViewBag.Title</h2>

@using (Html.BeginForm("Create", "User", FormMethod.Post))
{
  @Html.AntiForgeryToken()

  <div class="form-group">
    <label for="username">Usuario</label>
    @Html.TextBoxFor(m => m.username, new { @class = "form-control", required = "required" })
  </div>

  <div class="form-group">
    <label for="hashed_password">Contraseña</label>
    @Html.PasswordFor(m => m.hashed_password, new { @class = "form-control", required = "required" })
  </div>

  <div class="form-group">
    <label for="enabled">Habilitado</label>
    @Html.CheckBoxFor(m => m.enabled, new { @class = "form-check-input" })
  </div>

  <button type="submit" class="btn btn-primary">Crear</button>
  <a href="@Url.Action("Index", "User")" class="btn btn-default">Cancelar</a>
}
